name: CI with Tests and Coverage

on: [push, pull_request]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y lcov build-essential g++-12

    - name: Configure CMake
      run: |
        mkdir -p build
        cd build
        cmake -DBUILD_TESTS=ON -DENABLE_COVERAGE=ON ..
        cd ..

    - name: Build project
      run: |
        cd build
        make -j$(nproc)

    - name: Run Google Tests
      run: |
        cd build/test
        ./tests --gtest_output=xml:test_results.xml

    - name: Generate coverage
      run: |
        cd build
        ./test/tests
        
        lcov --capture --directory . --output-file coverage.info
        lcov --remove coverage.info '*/test/*' '*/third-party/*' '*/mock/*' -o filtered.info
        lcov --extract filtered.info '*/banking/*' '*/src/*' -o result.info
        

    - name: Upload coverage to Coveralls
      uses: coverallsapp/github-action@v2
      with:
        path-to-lcov: build/result.info
        github-token: ${{ secrets.GITHUB_TOKEN }}

    - name: Upload test results
      uses: actions/upload-artifact@v3
      with:
        name: test-results
        path: build/test/test_results.xml

    - name: Upload coverage report
      uses: actions/upload-artifact@v3
      with:
        name: coverage-report
        path: build/coverage-report
